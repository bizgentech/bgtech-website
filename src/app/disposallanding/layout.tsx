// Standalone layout — intentionally excludes the global Header and Footer
export default function DisposalLandingLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
